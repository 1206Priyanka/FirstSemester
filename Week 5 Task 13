basenumbers = []
data = {}
basenumber = 0
while basenumber < 5:
    basenumber = basenumber+1
    basenumbers.append(basenumber)
    exponent = 0
    while exponent < 5:
        exponent = exponent+1
        result = basenumber**exponent
        if basenumber not in data:  
            data[basenumber] = [result]
        else:
            data[basenumber].append(result)

for row in zip(*([key] + (value) for key, value in sorted(data.items()))):
    print(*row)

        
